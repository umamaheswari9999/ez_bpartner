import 'package:flutter/material.dart';

class LoanReferralWidget extends StatelessWidget {
  const LoanReferralWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Loan '),
    );
  }
}
